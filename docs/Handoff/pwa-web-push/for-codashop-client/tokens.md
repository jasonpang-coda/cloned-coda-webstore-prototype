# Token crosswalk — PWA Install + Web Push

Generated by `scripts/token-crosswalk.mjs` against production token files under
`../../../codapayments-codashop-client-09ce85cb9b88/assets/css/sitebuilder/tokens/`.

**Summary:** 2 RENAME · 3 NEW_SEMANTIC · 15 EXACT · 1 NEW_EFFECT (21 total)

| Token | Class | Production name | Action |
|---|---|---|---|
| `--border-weight-default` | RENAME | `--x-sys-stroke-thin` | Use `--x-sys-stroke-thin`. Production expresses border weight via the --x-sys-stroke-* scale (thin/medium/thick) rather than a single default. `thin` is the visual match for every prototype usage seen so far. |
| `--x-bg-action-neutral` | **NEW_SEMANTIC** (gated) | _none_ | GATED — design-system owner decides. Proposed: add to assets/css/sitebuilder/tokens/background/_action.scss (theme-preset-edge branch). Default path until sign-off: use `--x-bg-action-secondary`. Production's bg-action-* family is primary/secondary/tertiary/destructive/positive/caution/inverse/subtle — no neutral slot. Needed for a control (e.g. a toggle thumb) that should read as inert, not as any of those semantic actions. |
| `--x-bg-control-active` | EXACT | `--x-bg-control-active` | Cite as-is. |
| `--x-bg-sheet` | EXACT | `--x-bg-sheet` | Cite as-is. |
| `--x-border-control-active` | EXACT | `--x-border-control-active` | Cite as-is. |
| `--x-gap-content-default` | EXACT | `--x-gap-content-default` | Cite as-is. |
| `--x-motion-control-press-scale` | RENAME | `--x-motion-sku-press-scale` | Use `--x-motion-sku-press-scale`. Same role (press-scale on a control) under production's sku-motion naming. No prototype-specific behaviour depends on the distinct name. |
| `--x-motion-sys-duration-slowest` | EXACT | `--x-motion-sys-duration-slowest` | Cite as-is. |
| `--x-motion-sys-ease-decelerate` | EXACT | `--x-motion-sys-ease-decelerate` | Cite as-is. |
| `--x-motion-toggle` | EXACT | `--x-motion-toggle` | Cite as-is. |
| `--x-pad-surface-l` | EXACT | `--x-pad-surface-l` | Cite as-is. |
| `--x-pad-surface-m` | EXACT | `--x-pad-surface-m` | Cite as-is. |
| `--x-pad-surface-xxs` | EXACT | `--x-pad-surface-xxs` | Cite as-is. |
| `--x-radius-badge-full` | EXACT | `--x-radius-badge-full` | Cite as-is. |
| `--x-shadow-story-card` | NEW_EFFECT | _none_ | Exempt from closed-set rule — may add directly. Proposed: assets/css/sitebuilder/tokens/system/_elevation.scss or a new effect.scss (theme-preset-edge branch). Fallback if you'd rather not add yet: `--x-sys-effect-shadow-neutral`. Effect/extension tier — exempt from the closed-set rule (web-store-tokens §2b explicitly carves out extensions/effect tokens). May be added directly rather than gated on design-system sign-off; fallback given anyway so a first pass can ship without waiting on it. |
| `--x-size-control-xs` | EXACT | `--x-size-control-xs` | Cite as-is. |
| `--x-size-icon-m` | EXACT | `--x-size-icon-m` | Cite as-is. |
| `--x-surface-ghost-2` | **NEW_SEMANTIC** (gated) | _none_ | GATED — design-system owner decides. Proposed: add to assets/css/sitebuilder/tokens/background/_overlay.scss (theme-preset-edge branch). Default path until sign-off: use `--x-bg-overlay-subtle`. Prototype's ghost-N scale (near-transparent white/black steps for track/scrim fills) has no production analogue; overlay tier is the closest existing family. |
| `--x-surface-ghost-4` | **NEW_SEMANTIC** (gated) | _none_ | GATED — design-system owner decides. Proposed: add to assets/css/sitebuilder/tokens/background/_overlay.scss (theme-preset-edge branch). Default path until sign-off: use `--x-bg-overlay-default`. Same family as --x-surface-ghost-2, one step more opaque (used for a border rather than a fill). |
| `--x-text-header-default` | EXACT | `--x-text-header-default` | Cite as-is. |
| `--x-text-hyperlink-default` | EXACT | `--x-text-hyperlink-default` | Cite as-is. |

## Gated — needs design-system owner sign-off before an engineer adds these

Per the prototype's `web-store-tokens` skill (§2b), the semantic tiers are a closed
set — adding to them is a design-system decision, not an implementer's call. Until
signed off, use the listed fallback.

- `--x-bg-action-neutral` → propose assets/css/sitebuilder/tokens/background/_action.scss (theme-preset-edge branch), fallback `--x-bg-action-secondary`.
- `--x-surface-ghost-2` → propose assets/css/sitebuilder/tokens/background/_overlay.scss (theme-preset-edge branch), fallback `--x-bg-overlay-subtle`.
- `--x-surface-ghost-4` → propose assets/css/sitebuilder/tokens/background/_overlay.scss (theme-preset-edge branch), fallback `--x-bg-overlay-default`.
