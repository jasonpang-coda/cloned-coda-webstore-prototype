# Release Notes — v0.102.1

**Date:** September 2026
**Milestone:** Hotfix — regenerate stale `sku-card` handoff spec

---

## Highlights

- **`docs/Handoff/components/sku-card/spec.md` was out of sync with its story.** `src/library/stories/SkuCard.stories.js` picked up the `items`-breakdown variant and a new usage rule (the `BundleItem` tag-pill padding-top rule, `REG-MTPTNN40`) in v0.102.0's carryover changes, but the exported handoff spec was never regenerated to match — `node scripts/export-handoff.mjs --story --verify` correctly caught the drift (`export-handoff.test.mjs`'s pre-existing failing test was real, not flaky).

## Changes

### `docs/Handoff/components/sku-card/spec.md`

- Regenerated via `node scripts/export-handoff.mjs --story sku-card`. `variants: 4` → `6` (added "With child breakdown" / "With child breakdown (row)"), +2 usage rules (the padding-top rule, and the `items` prop's `BundleBreakdown` row description).

## Verification

- `npm run handoff:verify` (`node scripts/export-handoff.mjs --story --verify`) — all 38 story specs `[PARITY]`, 0 drift.
- `npm test` — 52/52 pass, 0 failures (the previously-failing `export-handoff.test.mjs` case now passes for real, not just incidentally).
