# Release Notes — v0.60.6

**Date:** August 2026
**Milestone:** Removed the terms & conditions link from the FCM info sheet's Buy Now footer

---

## Highlights

- **Cleaner signed-in footer.** `ItemSummarySheet`'s FCM `footer: 'buyNow'`
  variant no longer shows a "View Buy Now Terms & Conditions" link — the
  signed-in footer is now just the price/CTA row + rewards line.
  `BuyNowBar`'s own terms link is a separate component and unaffected.

## Changes

### `src/components/ItemSummarySheet.vue`

- Removed the `<a class="isum__buynow-terms" ...>` link from the `canBuy`
  branch of the FCM buy-now footer.
- Removed the now-unused `.isum__buynow-terms` CSS rule.

## Verification

- `node_modules/.bin/vite build` passes.
- Not yet exercised in a live browser session (sandbox blocks automated
  preview here) — visual confirmation of the footer needs `npm run dev` on
  the FCM theme.
