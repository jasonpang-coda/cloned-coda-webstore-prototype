# Release Notes — v0.68.2

**Date:** August 2026
**Milestone:** Diablo Immortal — SEO section copy reformatted into scannable paragraphs

---

## Highlights

- **SEO body copy, no longer a wall of text.** `strings.page.seo.body` is now
  rich HTML (`v-html`, same trusted-copy precedent as `CategoryBanner`'s
  `description` / `StepDetails`' `termsBody`) split into 4 short paragraphs
  with bolded key terms (Warlock, The Soulgorger, seamless cross-platform
  play, Warband) instead of one unbroken 200-word block.

## Changes

### `src/App.vue`

- SEO body element switched from `<p>{{ }}</p>` to a `v-html`-bound `<div>`.
- New `.seo-content__body :deep(p)` / `:deep(strong)` rules — paragraph gaps
  and header-coloured bold emphasis (same carve-out as `CategoryBanner`'s
  `:deep(strong)` rule; the DS has no standalone weight token for inline
  emphasis).

### `src/stores/diabloimmortal/store.js`

- `strings.page.seo.body` rewritten as 4 `<p>`/`<strong>` HTML paragraphs.
